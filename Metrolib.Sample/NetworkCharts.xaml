<UserControl x:Class="Metrolib.Sample.NetworkCharts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:network="clr-namespace:Metrolib.Controls.Charts.Network;assembly=Metrolib"
             xmlns:sample="clr-namespace:Metrolib.Sample"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <network:NetworkChart Nodes="{Binding Avengers}"
                              Edges="{Binding Dislikes}">
            <network:NetworkChart.Layout>
                <network:ForceDirectedLayout />
            </network:NetworkChart.Layout>
            <network:NetworkChart.NodeTemplate>
                <DataTemplate DataType="{x:Type sample:AvengerViewModel}">
                    <Border BorderBrush="Black"
                            BorderThickness="1">
                        <StackPanel Orientation="Vertical">
                            <Image Source="{Binding Portrait}"
                                   Width="48"
                                   Height="48"
                                   RenderOptions.BitmapScalingMode="HighQuality" />
                            <TextBlock Text="{Binding Name}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </network:NetworkChart.NodeTemplate>
        </network:NetworkChart>
    </Grid>
</UserControl>